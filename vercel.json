{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "regions": ["iad1"],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':(exclude)doc'",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
